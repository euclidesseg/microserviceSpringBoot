server:
    port: 8081
spring:
    cloud:
        config:
            server:
                git:
                    default-label: multiple-instance
                    uri: https://github.com/euclidesseg/microserviceSpringBoot
                    search-paths: config-data
    application:
        name: config-server 